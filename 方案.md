# 前端改版方案

## 1. 项目现状

- 主前端项目在 `vue-tlias-management`，技术栈是 `Vue 3 + Vite + Element Plus + ECharts`。
- 后端主项目在 `web-ai-project02/tlias-web-management`，前端通过 `/api` 调用业务接口，通过 `/api/chat` 挂载 AI 聊天组件。
- 当前前端主要页面包括：
  - 登录页：`src/views/login/index.vue`
  - 主框架页：`src/views/layout/index.vue`
  - 首页仪表盘：`src/views/index/index.vue`
  - 业务页面：`dept / clazz / stu / emp / role / log / report`
- 权限控制已经接入：
  - 路由权限在 `src/router/index.js`
  - 菜单权限在 `src/utils/menu.js`
- 当前全局样式较少，`src/assets/main.css` 只有基础 `margin` 清零，页面视觉主要分散在各个 `.vue` 文件中。

## 2. 当前界面问题

- 登录页视觉较旧，且使用的是本地旧图 `src/assets/bg1.jpg`。
- 登录页容器高度写死为 `410px`，背景展示不够完整，不适合大屏。
- 主框架页顶部、侧边栏、内容区样式较朴素，缺少统一的色彩、间距和层级。
- 首页统计卡片和图表可用，但大量颜色直接写在模板里，复用性和一致性较差。
- 列表页大多是“标题 + 按钮 + 表格 + 弹窗”的基础结构，功能完整，但视觉风格不统一。
- 侧边栏高度写死为 `730px`，在不同分辨率下容易出现留白或不贴底的问题。

## 3. 本次改版目标

- 在不改动后端接口、权限逻辑、菜单结构的前提下，提升前端整体观感。
- 统一登录页、主框架页、首页和各业务页的视觉风格。
- 让系统更符合“教育管理系统”的场景感，而不是纯默认后台风格。
- 完成指定图片替换，并把它纳入新的前端视觉方案。

## 4. 指定图片替换方案

### 4.1 图片信息

- 新图片路径：
  `C:\Data\project\tlias---javaee3\head\v2-f3ea5ca8859b092b89da66d0e1a06a0f_1440w.jpg`

### 4.2 使用方式

- 这张图建议作为登录页主背景图使用。
- 如首页需要强化品牌感，也可以在首页顶部加一块横幅区域，复用同一张图做轻量化裁切展示。

### 4.3 前端引用方式

- 这张图不建议再复制进 `vue-tlias-management/src/assets`。
- 当前后端已经配置了 `/head/**` 静态资源映射，Vite 也已经把 `/head` 代理到 `http://localhost:8080`。
- 因此前端可以直接使用：

```css
background-image: url('/head/v2-f3ea5ca8859b092b89da66d0e1a06a0f_1440w.jpg');
```

- 这样做的好处：
  - 不需要重复保存图片
  - 前后端开发环境和后续部署路径更统一
  - 与当前员工头像等 `head` 目录资源的管理方式保持一致

## 5. 页面改版建议

### 5.1 登录页

目标文件：`vue-tlias-management/src/views/login/index.vue`

建议改动：

- 将旧背景图 `../../assets/bg1.jpg` 替换为新图 `/head/v2-f3ea5ca8859b092b89da66d0e1a06a0f_1440w.jpg`
- 背景图上增加半透明遮罩，避免表单与背景混在一起
- 登录区域改为全屏垂直居中，不再使用固定 `410px` 高度
- 登录卡片改为更轻的玻璃拟态或白色半透明卡片
- 标题区增加副标题，例如“Teaching & Learning Information Administration System”
- 验证码输入区、按钮宽度、间距做统一优化

建议效果：

- 首页打开时先看到图书馆/书籍氛围图，和教育主题更贴近
- 登录框更聚焦，视觉上更现代

### 5.2 主框架页

目标文件：`vue-tlias-management/src/views/layout/index.vue`

建议改动：

- 头部保留系统名称和用户信息，但重新设计配色和排版
- 侧边栏改成更稳定的后台导航样式，取消固定 `730px` 高度，改为随视口撑满
- 激活菜单、悬停菜单、分组标题做统一视觉强调
- 内容区增加内边距、背景层次和卡片容器，减少“页面内容直接贴在白底上”的感觉
- 保留现有 AI 聊天组件 `<open-chat-widget url="/api/chat">`，但要检查其层级不要被新布局遮挡

### 5.3 首页仪表盘

目标文件：`vue-tlias-management/src/views/index/index.vue`

建议改动：

- 将顶部改成“欢迎区 + 关键指标卡片”的结构
- 统计卡片颜色不要再直接写在模板内，改为统一 CSS 变量或 class
- 图表卡片增加标题区、留白和统一圆角
- 可以在欢迎区加入教育主题文案，和登录页新图形成统一视觉语言
- 若复用新图片，建议只做顶部横幅，不要整页铺满，避免影响图表可读性

### 5.4 业务管理页

目标页面：

- `dept`
- `clazz`
- `stu`
- `emp`
- `role`
- `log`
- `report`

建议改动：

- 统一页面头部结构：标题、说明、操作按钮分区
- 查询区、表格区、分页区改成统一卡片风格
- 按钮颜色、表格表头、弹窗标题、表单间距做统一规范
- 对“新增”“编辑”“删除”等高频动作建立统一视觉反馈
- 先改一页作为模板，再横向复制到其他页面，避免每页单独散改

## 6. 样式落地建议

目标文件：`vue-tlias-management/src/assets/main.css`

建议新增全局设计变量，例如：

- 主色
- 辅助色
- 页面背景色
- 卡片圆角
- 阴影
- 标题字号
- 内容间距

建议原则：

- 公共视觉规则尽量放到 `main.css`
- 页面特有样式保留在各自 `.vue` 文件中
- 避免继续增加大量内联 `style`

## 7. 推荐实施顺序

1. 先整理全局样式变量和页面基础间距规范
2. 优先改登录页，并替换指定图片
3. 改主框架页的头部、侧边栏、内容容器
4. 改首页仪表盘，让首页先形成新的整体风格
5. 选一个列表页做模板页
6. 将模板样式平移到其他管理页
7. 最后做联调和细节修正

## 8. 验收标准

- 登录页背景图已替换为：
  `/head/v2-f3ea5ca8859b092b89da66d0e1a06a0f_1440w.jpg`
- 登录页在常见桌面分辨率下显示正常，不拉伸、不留大片空白
- 主框架页头部、侧边栏、内容区风格统一
- 首页卡片和图表样式统一，视觉层次比当前版本更清晰
- 各业务页风格一致，但不影响原有增删改查功能
- 路由权限、菜单权限、聊天组件功能保持正常

## 9. 本次改版范围说明

本次方案默认以“前端样式优化和图片替换”为主，不主动改动以下内容：

- 后端接口
- 数据库结构
- 权限模型
- 菜单权限逻辑
- AI 聊天接口协议

如果后续需要，我可以直接按这份方案继续落地代码，先从登录页和主框架页开始改。
