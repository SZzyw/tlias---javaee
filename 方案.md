# OpenChatWidget 嵌入方案

## 一、现状分析

| 项目 | 技术栈 | 位置 |
|------|--------|------|
| **主项目 (tlias-javaee)** | Vue 3 + Vite 3 + Element Plus + Spring Boot 3 | `vue-tlias-management/` + `web-ai-project02/` |
| **OpenChatWidget** | React 19 + Vite 5 + Tailwind CSS 4 (ES Module 库) | `openchatwidget/sdk/` |

**核心矛盾：主项目前端是 Vue 3，OpenChatWidget 是 React 组件。** 二者不能直接在同一个组件树中渲染，需要桥接。

**没有现成的 AI/Chat 功能** — 后端需要新增 chat API，或独立部署 AI Agent 服务。

---

## 二、嵌入方案选型

### 方案 A：Custom Element（Web Component）桥接 ★ 推荐

在 Vue 项目中创建一个薄层，用 `react-dom` 将 React widget 渲染到一个 DOM 容器中，再注册为自定义 HTML 元素，供 Vue 使用。

```
Vue 组件  ──>  <open-chat-widget>  ──>  ReactDOM.createRoot()  ──>  <OpenChatWidget />
```

**优点：**
- Vue 和 React 完全解耦，各自独立运行
- 自定义元素是浏览器标准，Vue 3 原生支持
- 生命周期清晰 (`connectedCallback` / `disconnectedCallback`)
- 支持 props 传递（通过 attribute 或 setter）

**缺点：**
- 需要在 Vue 项目中安装 React 18+ 和 ReactDOM（~130KB gzipped）
- 两个框架共存，打包体积增加
- 需要处理好 CSS 隔离（Widget 已用 `ocw:` 前缀 + 内联样式，问题较小）

### 方案 B：独立子应用 + Iframe

将 widget 部署为一个独立的 HTML 页面（React 微应用），在主项目中用 iframe 嵌入。

**优点：**
- 完全隔离，无框架/样式冲突
- 不需要在 Vue 项目里装 React

**缺点：**
- 固定定位的浮动气泡在 iframe 中无法超出 iframe 边界
- 通信不便是用 postMessage
- 移动端适配困难

**结论：❌ 不推荐 — 与 widget 的浮动气泡定位方式不兼容。**

### 方案 C：预构建独立 Bundle + script 标签

将 OpenChatWidget 及其所有依赖（React、ReactDOM、AI SDK 等）打包为一个 IIFE/UMD 文件，直接在 `index.html` 中通过 `<script>` 加载。

**优点：**
- 无需修改 Vite 构建流程
- 全局可用

**缺点：**
- 维护困难，升级需要重新构建 bundle
- 无法 tree-shaking
- 全局污染

**结论：可作备选，但不推荐作为主要方案。**

---

## 三、选定方案：Custom Element 桥接

### 3.1 整体架构

```
┌─────────────────────────────────────────────────────────────┐
│                    Vue 3 SPA (主项目)                        │
│  ┌──────────┐  ┌──────────┐  ┌──────────────────────────┐  │
│  │ Page A   │  │ Page B   │  │ 任意页面 (或专用页面)      │  │
│  │          │  │          │  │  <open-chat-widget />     │  │
│  └──────────┘  └──────────┘  └──────────────────────────┘  │
│                                      │                      │
│                            ┌─────────▼──────────┐          │
│                            │  Custom Element    │          │
│                            │  (web-component)   │          │
│                            │  react-dom mount   │          │
│                            └─────────┬──────────┘          │
│                                      │                      │
│                            ┌─────────▼──────────┐          │
│                            │  OpenChatWidget    │          │
│                            │  (React 组件)       │          │
│                            └─────────┬──────────┘          │
│                                      │                      │
│                            ┌─────────▼──────────┐          │
│                            │  useChat (AI SDK)  │          │
│                            │  → POST /api/chat  │          │
│                            └─────────┬──────────┘          │
└──────────────────────────────────────┼──────────────────────┘
                                       │ HTTP
┌──────────────────────────────────────▼──────────────────────┐
│              Spring Boot / Node.js Agent Server              │
│              /api/chat  (Vercel AI 流式协议)                  │
└─────────────────────────────────────────────────────────────┘
```

### 3.2 实施步骤

#### Step 1：后端 — 新增 Chat API

**方式 A (推荐)：独立 Node.js Agent Service**

OpenChatWidget 需要后端符合 Vercel AI SDK 的流式通信协议。Spring Boot 实现此协议较复杂，推荐另起一个 Node.js 服务：

- 位置：`web-ai-project02/chat-agent/` （或根目录 `chat-agent/`）
- 使用 Hono + Vercel AI SDK（openchatwidget 的 sandbox 已有参考实现）
- 端口 8787，提供 `POST /api/chat` 端点
- 前端 Vite 代理 `/api` 到 Spring Boot，再配置 `/chat` 代理到 Node.js；或单独端口

或者，如果希望减少运维复杂度：

**方式 B：Spring Boot Controller + SSE**

在 `tlias-web-management` 中新增：
- `ChatController.java` — 接收消息，调用 OpenAI/其他 LLM API，返回 SSE 流
- 需要实现 Vercel AI SDK Data Stream Protocol（参考 https://sdk.vercel.ai/docs/ai-sdk-ui/stream-protocol）

#### Step 2：前端 — 构建 Custom Element 桥接文件

在 `vue-tlias-management/` 中：

1. **安装依赖**（作为 devDependencies 或 dependencies）：
   ```bash
   npm install react react-dom
   npm install @openchatwidget/sdk  # 从本地路径或 npm 安装
   ```

2. **创建桥接文件** `src/widget-bridge.ts`：
   - 导入 `React`, `createRoot`, `OpenChatWidget`
   - 定义 `class OpenChatWidgetElement extends HTMLElement`
   - 在 `connectedCallback` 中用 `createRoot` 渲染
   - 在 `disconnectedCallback` 中调用 `root.unmount()`
   - 注册为 `customElements.define('open-chat-widget', ...)`

3. **在主入口加载桥接**：
   - 在 `src/main.js` 中 `import './widget-bridge'`
   - 确保注册发生在 Vue app mount 之前

#### Step 3：前端 — 集成到 Vue 页面

有两种使用方式（可选一种或结合）：

**方式 A：全局浮动气泡（跨所有页面）**

修改 `src/views/layout/index.vue`，在布局模板中添加 `<open-chat-widget url="...">`，使其在所有页面可见。

**方式 B：专用聊天页面**

新增路由 `/chat`，在对应 Vue 组件中使用 `<open-chat-widget url="...">`

#### Step 4：CSS 隔离处理

OpenChatWidget 已自带隔离措施，无需额外处理：
- Tailwind 类名使用 `ocw:` 前缀
- 关键样式使用内联 `style={}`
- 主题 CSS 通过 `<style>` 注入并限定在 `[data-openchatwidget-root]` 内部

可能需调整 Vite 配置确保 Tailwind 的 `ocw:` 前缀不冲突。

#### Step 5：构建与部署

- 开发环境：`npm run dev`（Vite 自动处理）
- 生产环境：`npm run build` 将 widget-bridge 一并打包
- 注意 Vite 3 和 Vite 5 的兼容性（当前主项目 Vite 3，Widget SDK 用 Vite 5，桥接文件在主项目构建即可）

---

## 四、风险与注意事项

| 风险 | 影响 | 缓解措施 |
|------|------|----------|
| Vue 3 + React 双框架共存 | 打包体积增大 ~130KB | 可接受（管理系统对体积不敏感） |
| Vite 版本差异（3 vs 5） | 构建配置冲突 | 桥接文件在主项目构建，不依赖 SDK 的 Vite |
| Widget 修改 body overflow | 与 Element Plus 弹窗冲突 | 测试后调整 z-index 和 overflow 行为 |
| 字体加载（Google Fonts） | 外网访问受限 | 替换为本地字体或使用系统字体 |
| 移动端响应式 | 全屏覆盖 Element Plus 布局 | 测试并调整断点 |

---

## 五、备选：不引入 React 的简化方案

如果希望完全不引入 React，可以：

1. **仅使用 Widget 的 CSS/HTML**：参考 Widget 的 UI 设计，用 Vue 3 + Element Plus 重新实现一个聊天界面
2. **仅使用后端协议**：复用 Vercel AI SDK 的流式协议，前端用 Vue 实现

此方案工作量较大（~3-5天），但无框架冲突，技术栈统一。如果团队对 React 不熟悉或对包体积敏感，可考虑此方向。

---

## 六、工作量估算

| 步骤 | 内容 | 工时 |
|------|------|------|
| 1 | 后端 Chat API（Node.js 独立服务） | 0.5-1 天 |
| 2 | 前端 Custom Element 桥接 | 2-4 小时 |
| 3 | Vue 集成（布局/路由） | 1-2 小时 |
| 4 | CSS 适配与测试 | 2-4 小时 |
| 5 | 联调与部署 | 0.5 天 |
| **合计** | | **2-3 天** |

---

## 七、推荐路径

1. **后端** → 新增独立 Node.js Chat Agent 服务（参考 `openchatwidget/examples/basic-react-express-app` 或 SDK 中的 sandbox）
2. **前端** → Custom Element 桥接文件 → Vue 布局中全局引入
3. **测试** → 跨页面兼容性、移动端、字体
