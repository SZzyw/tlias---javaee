# Tlias 教育管理系统 — 改进方案

## 项目现状简述

- **后端**：Spring Boot 3 + MyBatis + JWT，实现了部门/员工/班级/学生的 CRUD，以及数据统计报表
- **前端**：Vue 3 + Element Plus + ECharts，包含登录、导航、各管理页面和 AI 聊天组件
- **已有特色**：AI 聊天助手、阿里云 OSS 上传、AOP 日志、违纪扣分
- **明显短板**：员工编辑/删除功能未绑定、批量删除未实现、日志页为空白、修改密码无实现、安全配置硬编码、无单元测试、无 API 文档

---

## 方案一：🔧 补全收尾 + 体验打磨（推荐起步）

> **定位**：先把已有的功能做完，把"半成品"变成"完整可用"。

| 模块 | 具体内容 | 前后端 |
|------|---------|--------|
| 员工编辑/删除 | 绑定前端按钮事件，走已有 `PUT /emps` 和 `DELETE /emps` 接口 | 前端为主 |
| 批量删除员工 | 获取表格选中项，调用已有批量删除接口 | 前端 |
| 批量删除学生 | 同上，调用已有接口 | 前端 |
| 日志页面 | 实现操作日志记录（AOP 拦截 + 数据库存储 + 前端展示） | 全栈 |
| 修改密码 | 用户头像下拉菜单中增加修改密码入口 | 全栈 |
| 搜索参数保留 | 编辑返回后保留当前搜索条件和页码 | 前端 |
| 加载状态 | 查询时增加 loading 动画 | 前端 |
| 密码加密 | 将明文密码改为 BCrypt 加密存储 | 后端 |
| JWT 密钥外置 | 硬编码的 `"way_ne"` 移到 `application.yml` | 后端 |
| 代码清理 | 删除无用的 `WebConfic`/`TokenInterceptor`，修复命名拼写 | 后端 |

> **工作量**：约 2-3 天
> **适合**：想先把作业/项目交付得完整无 bug

---

## 方案二：📊 数据分析 + 可视化强化

> **定位**：在现有报表基础上大幅增强数据能力，让"数据统计"成为亮点。

| 模块 | 具体内容 | 前后端 |
|------|---------|--------|
| 仪表盘首页 | 把当前空白首页改为数据驾驶舱：总人数/班级数/男女生比例/违纪排行等 | 全栈 |
| 趋势图 | 按月度展示员工入职趋势、学生入学趋势 | 全栈 |
| 违纪统计 | 按班级展示违纪次数排行，支持导出 | 全栈 |
| 导出 Excel | 员工/学生列表导出为 Excel（Apache POI 或 EasyExcel） | 全栈 |
| 打印功能 | 关键报表支持打印/导出 PDF | 前端 |
| 数据下钻 | 点击图表可进入对应列表（如点击班级人数柱状图进入学生列表并过滤） | 全栈 |

> **工作量**：约 3-5 天
> **适合**：想展示数据处理能力，给老师看"丰富的数据看板"

---

## 方案三：🛡️ 安全加固 + 权限管理

> **定位**：从"玩具"升级为"真正可用的系统"，加入权限控制。

| 模块 | 具体内容 | 前后端 |
|------|---------|--------|
| 角色管理 | 添加角色表（管理员/班主任/讲师/学工），支持 CRUD | 全栈 |
| 权限分配 | 给角色分配菜单/按钮权限，后端接口级拦截 | 全栈 |
| 用户-角色关联 | 员工表关联角色，登录后返回权限列表 | 全栈 |
| 前端路由权限 | 根据角色动态生成侧边栏菜单 | 前端 |
| 操作日志审计 | 记录谁在什么时间做了什么操作，可追溯 | 全栈 |
| 登录验证码 | 增加图形验证码或滑块验证码 | 全栈 |
| 接口防重刷 | 基于 Redis 的接口幂等性 / 限流 | 后端 |

> **工作量**：约 5-7 天
> **适合**：想展示"企业级开发能力"，有足够时间

---

## 方案四：🚀 性能 + 架构升级

> **定位**：引入缓存和中间件，向生产级架构靠拢。

| 模块 | 具体内容 | 前后端 |
|------|---------|--------|
| Redis 缓存 | 将现有独立 Redis 模块整合到主项目，缓存部门列表/班级列表等热点数据 | 后端 |
| 缓存预热 | 项目启动时自动加载热点数据到 Redis | 后端 |
| 接口文档 | 集成 Knife4j / SpringDoc，生成 Swagger 文档 | 后端 |
| Docker 部署 | 编写 Dockerfile + docker-compose（MySQL + Redis + 后端 + 前端） | 运维 |
| CI/CD | 配置 GitHub Actions 自动构建和部署 | 运维 |
| 数据库索引 | 分析慢查询，补充缺失索引 | 后端 |
| 统一异常状态码 | 完善错误码体系，区分业务异常和系统异常 | 后端 |

> **工作量**：约 3-5 天
> **适合**：想展示"工程化能力"，为简历加亮点

---

## 方案五：💡 功能创新（加分特色）

> **定位**：做老师的项目没有的功能，脱颖而出。

| 模块 | 具体内容 | 前后端 |
|------|---------|--------|
| 排课系统 | 班级-课程-教师关联，支持按周排课/调课 | 全栈 |
| 考勤签到 | 学生扫码/手动签到，自动统计出勤率 | 全栈 |
| 成绩管理 | 学生成绩录入、班级均分、排名、成绩分布图 | 全栈 |
| 通知公告 | 系统公告发布，前端弹窗/红点提醒 | 全栈 |
| 学生家长端 | 家长微信小程序/轻量 H5 查看孩子考勤和成绩 | 全栈 |
| 消息推送 | 钉钉/企业微信机器人通知（违纪提醒、开班通知） | 后端 |

> **工作量**：每个子功能约 3-5 天
> **适合**：想做出"项目亮点"用于面试或比赛

---

## 我的建议

1. **优先方案一** — 先把已有功能彻底做完，不要让老师看到空按钮、空页面。
2. **叠加方案二中的仪表盘** — 把首页从空白变成数据看板，印象分会大幅提升。
3. **有余力再做方案三或方案四其中一项** — 权限或 Docker，都是能写到简历上的硬技能。

> 具体先做哪个你来定，选好之后我帮你细化 task 并逐步实现。
