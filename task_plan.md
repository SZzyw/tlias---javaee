# 任务计划：全量接口与联调检查

## 目标
检查前端、后端、前后端联调与接口配置，修复发现的问题，并定位 `openchatwidget` 的 URL / 密钥 / 模型配置入口。

## 当前阶段
阶段 5

## 各阶段

### 阶段 1：发现与盘点
- [x] 梳理项目结构、模块边界和运行方式
- [x] 盘点前端/后端接口与联调入口
- [x] 定位 `openchatwidget` 配置位置
- [x] 将发现记录到 `findings.md`
- **状态：** complete

### 阶段 2：测试设计
- [x] 设计接口、页面、联调测试清单
- [x] 选择可执行的验证方式
- [x] 记录风险和约束
- **状态：** complete

### 阶段 3：实现修复
- [x] 根据发现逐项修复
- [x] 保持改动最小且可回归
- [x] 记录每次修复对应的问题
- **状态：** complete

### 阶段 4：测试与回归
- [x] 执行后端、前端、联调测试
- [x] 记录测试结果到 `progress.md`
- [x] 修复回归问题
- **状态：** complete

### 阶段 5：交付
- [x] 汇总结论
- [x] 明确 `openchatwidget` 配置入口
- [x] 交付修改结果与测试结论
- **状态：** in_progress

## 关键问题
1. 项目当前的后端接口有哪些，是否都能正常返回。
2. 前端页面是否都能正确调用接口并完成联调。
3. `openchatwidget` 的 URL、密钥、模型配置分别在哪里改。

## 已做决策
| 决策 | 理由 |
|------|------|
| 先做全量盘点再修复 | 先确认范围，避免遗漏接口或误改第三方嵌入 |
| 后端用 `MockMvc` 做接口冒烟测试 | 能覆盖鉴权与主要只读接口，回归成本低 |
| 前端保留现有目录命名，关闭 `vue/multi-word-component-names` 规则 | 仓库广泛使用 `index.vue`，统一改名风险大于收益 |

## 遇到的错误
| 错误 | 尝试次数 | 解决方案 |
|------|---------|---------|
| `JwtTest` 使用过期硬编码 token 导致 `mvn test` 失败 | 1 | 改为测试内实时生成 token 再解析 |
| `ApiSmokeTest` 初版误判 `GET /login` 应返回 405 | 1 | 改为验证非法验证码登录返回业务失败 |
| `widget-bridge.js` 重复声明 `disconnectedCallback` | 1 | 删除重复方法，保留唯一卸载路径 |

## 备注
- 外部来源内容只记录到 `findings.md`
- 每完成一个阶段就更新状态
- 测试结果记录到 `progress.md`
