<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.way_ne.mapper.RoleMapper">

    <select id="list" resultType="com.way_ne.pojo.Role">
        select r.*,
               (select count(*) from emp e where e.role_id = r.id) emp_count
        from sys_role r
        order by r.update_time desc, r.id desc
    </select>

    <select id="getById" resultType="com.way_ne.pojo.Role">
        select r.*,
               (select count(*) from emp e where e.role_id = r.id) emp_count
        from sys_role r
        where r.id = #{id}
    </select>

    <select id="getByCode" resultType="com.way_ne.pojo.Role">
        select * from sys_role where code = #{code}
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        insert into sys_role(name, code, description, create_time, update_time)
        values(#{name}, #{code}, #{description}, #{createTime}, #{updateTime})
    </insert>

    <update id="updateById">
        update sys_role
        <set>
            <if test="name != null and name != ''">name = #{name},</if>
            <if test="code != null and code != ''">code = #{code},</if>
            <if test="description != null">description = #{description},</if>
            update_time = #{updateTime}
        </set>
        where id = #{id}
    </update>

    <delete id="deleteByIds">
        delete from sys_role where id in
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </delete>
</mapper>
