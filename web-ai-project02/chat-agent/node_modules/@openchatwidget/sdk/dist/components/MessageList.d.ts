import * as React from "react";
import type { UIMessage } from "ai";
type WidgetMessageListProps = {
    messages: ReadonlyArray<UIMessage>;
    isGenerating: boolean;
    error?: Error;
    messageContainerRef: React.RefObject<HTMLDivElement | null>;
    isMobileViewport: boolean;
    disableReasoning?: boolean;
    renderMarkdown: (text: string) => React.ReactNode;
    emptyState?: React.ReactNode;
    onRespondToToolApproval?: (approval: {
        id: string;
        approved: boolean;
        reason?: string;
    }) => void;
};
export declare function MessageList({ messages, isGenerating, error, messageContainerRef, isMobileViewport, disableReasoning, renderMarkdown, emptyState, onRespondToToolApproval, }: WidgetMessageListProps): import("react/jsx-runtime").JSX.Element;
export {};
//# sourceMappingURL=MessageList.d.ts.map